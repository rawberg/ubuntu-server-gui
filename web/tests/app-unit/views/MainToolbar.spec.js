define(function (requirejs) {
    var MainToolbar = requirejs('views/MainToolbar'),
        ServerList = requirejs('collections/ServerList'),
        Server = requirejs('models/Server');

    xdescribe('MainToolbar - ItemView', function() {

    });
});