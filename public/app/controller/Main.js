// Generated by CoffeeScript 1.3.3

define(['underscore', 'backbone', 'app', 'view/dashboard/Dashboard', 'view/loginsignup/LoginSignup'], function(_, Backbone, App, Dashboard, LoginSignup) {
  /**
   * @class Main
   * Main entry point for the application.
   * @extends Backbone.Events
  */

  var Main;
  return Main = (function() {

    _.extend(Main.prototype, Backbone.Events);

    /**
     * @constructor
     * Creates a new Main controller.
     * @param {Object} [options] config options for the controller.
     * @return {Object} Main controller instance.
    */


    function Main(options) {
      this.App = App;
      this.views = {};
      return this;
    }

    /**
     * @method
     * Creates the application dashboard.
     * @return {Object} Main controller instance.
    */


    Main.prototype.dashboard = function() {
      var dashboardLayout;
      dashboardLayout = new Dashboard();
      this.App.mainViewport.show(dashboardLayout);
      return this;
    };

    /**
     * @method
     * Creates the application login screen.
     * @return {Object} Main controller instance.
    */


    Main.prototype.login = function() {
      this.App.mainToolbar.close();
      return this;
    };

    Main.prototype.onSubmitLogin = function(user) {};

    return Main;

  })();
});
